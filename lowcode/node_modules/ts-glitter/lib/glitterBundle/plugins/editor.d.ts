import { GVC } from '../../glitterBundle/GVController.js';
export declare class Editor {
    generateForm: (data: any, window: Window, callback: (res?: any) => void, second?: boolean) => string;
    constructor(gvc: GVC);
}
